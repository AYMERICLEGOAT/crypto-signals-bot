A-FAIRE-CHECKLIST.txt
_ancien-crypto-signals-bot.env
.env
ops_routine.log
signals/data/

# Secrets locaux : ces fichiers contiennent des jetons réels (bot Telegram,
# clé secrète Supabase, clé privée Polygon). Le dépôt étant PUBLIC, un
# `git add -A` distrait suffirait à les exposer définitivement. Seuls les
# .example, sans valeur réelle, sont suivis.
.dev.vars
**/.dev.vars
*.env
!.env.example
traffic/directory_kit.json

# Caches de données de marché : reconstructibles depuis les API publiques,
# et trop volumineux pour le dépôt (historique journalier + financement).
signals/data/long_daily/
signals/data/large_daily/
signals/data/funding/
signals/counts_live.json
